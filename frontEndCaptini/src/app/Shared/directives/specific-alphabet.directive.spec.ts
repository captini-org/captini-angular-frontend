import { SpecificAlphabetDirective } from './specific-alphabet.directive';

describe('SpecificAlphabetDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecificAlphabetDirective();
    expect(directive).toBeTruthy();
  });
});
